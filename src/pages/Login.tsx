import React from 'react';
import styled, {ThemeContext} from "styled-components";

const LoginContainer = styled.div`
`;

function Login() {
    return (
        <LoginContainer>로그인화면입니다.</LoginContainer>
    );
}

export default Login;


